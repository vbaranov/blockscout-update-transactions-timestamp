{
  "name": "blockscout-update-transactions-timestamp",
  "version": "1.0.0",
  "description": "Script for filling block timestamp in Blockscout transactions table in batches as a part of Blockscout DB denormalization for the sake of queries performance",
  "type":"module",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "pg": "^8.7.3"
  }
}
